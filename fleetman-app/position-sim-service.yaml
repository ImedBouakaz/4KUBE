apiVersion: v1
kind: Service
metadata:
  name: position-sim-service
  namespace: fleetman
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: position-sim-deployment
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 30010