apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: fleetman
spec:
  type: ClusterIP
  selector:
    app: api-gateway
  ports:
    - protocol: TCP
      port: 30020
      targetPort: 8080