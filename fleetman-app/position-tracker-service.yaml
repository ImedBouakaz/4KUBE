apiVersion: v1
kind: Service
metadata:
  name: position-tracker
  namespace: fleetman
spec:
  selector:
    app: position-tracker
  ports:
    - protocol: TCP
      port: 30010
      targetPort: 8080
  type: ClusterIP
