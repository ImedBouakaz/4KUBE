apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-mongo
  namespace: mongo
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual
  hostPath:
    path: /srv/k8s/mongo   
